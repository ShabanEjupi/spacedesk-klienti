<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.carriez.flutter_hbb">

    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 🚦 09-09-2026 — SpaceDesk v1 eshte VETEM KONTROLLUES.
         Cdo leje me poshte e detyron nje deklarate me VIDEO te Play Console, dhe
         asnje prej tyre nuk i duhet kontrolluesit: ato i takojne anes qe KONTROLLOHET
         (regjistrimi i ekranit, mikrofoni, sherbimi ne plan te pare, dritarja pluskuese).
         `tools:node="remove"` nuk eshte zbukurim: pa te nje plugin Flutter i rikthen
         ne manifestin E BASHKUAR, dhe burimi duket i paster nderkohe qe AAB-ja jo
         ([[play-sherbimet-ne-plan-te-pare]]). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" tools:node="remove" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" tools:node="remove" />
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" tools:node="remove" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" tools:node="remove" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" tools:node="remove" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" tools:node="remove" />

    <queries>
        <intent>
            <!-- https://developer.android.com/training/package-visibility/use-cases#open-urls-custom-tabs -->
            <action android:name="android.support.customtabs.action.CustomTabsService" />
        </intent>
    </queries>

    <application
        android:name=".MainApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="SpaceDesk"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true">

        <!-- 🚨 08-09-2026: InputService (AccessibilityService) është HEQUR nga v1.
             Play e kërkon një deklaratë të veçantë me video demo për Accessibility API,
             dhe ajo është e vetmja pengesë që s'kalohet me tekst. SpaceDesk-u i v1-shit
             është KONTROLLUES: hap desktopin e kompjuterit tënd nga telefoni — pikërisht
             ç'bën edhe klienti iOS. Kontrolli i KËTIJ telefoni nga larg kthehet te një
             version i mëpasëm, bashkë me deklaratën. Kodi i shërbimit rri te depoja. -->

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode|navigation"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Intent for deep linking-->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="rustdesk" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PermissionRequestTransparentActivity"
            android:excludeFromRecents="true"
            android:theme="@style/Transparent" />

        <!-- 🚦 MainService (specialUse|mediaProjection|microphone), FloatingWindowService dhe
             BootReceiver jane HEQUR me 09-09-2026. Ato ekzistojne vetem per anen qe
             KONTROLLOHET; SpaceDesk-u i celularit kontrollon, nuk kontrollohet.
             Kodi i tyre rri te depoja — kthehen te nje version i mepasem bashke me
             deklaratat me video, nese ndonjehere duhet. -->

        <!--
 Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java
        -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

</manifest>
